/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { DocAddEditComponent } from './doc-add-edit.component';

describe('DocAddEditComponent', () => {
  let component: DocAddEditComponent;
  let fixture: ComponentFixture<DocAddEditComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [DocAddEditComponent]
    });
    fixture = TestBed.createComponent(DocAddEditComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
