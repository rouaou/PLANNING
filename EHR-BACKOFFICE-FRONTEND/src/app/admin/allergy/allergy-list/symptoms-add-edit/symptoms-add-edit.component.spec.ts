/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SymptomsAddEditComponent } from './symptoms-add-edit.component';

describe('SymptomsAddEditComponent', () => {
  let component: SymptomsAddEditComponent;
  let fixture: ComponentFixture<SymptomsAddEditComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [SymptomsAddEditComponent]
    });
    fixture = TestBed.createComponent(SymptomsAddEditComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
